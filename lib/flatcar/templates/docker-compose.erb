webapp:
  build: .
  ports:
    - "3000:3000"
  volumes:
    - .:/usr/src/app
  working_dir: /usr/src/app
  command: bundle exec rails s -b '0.0.0.0'
  <%= service_link %>

<%= data_volume_service %>

<%= database_service %>
